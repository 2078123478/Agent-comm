请修复 AlphaOS 审计发现的 2 个 Critical 问题：

## 🔴 Critical 1: Quote validation 缺失
**位置**：
- `src/skills/alphaos/runtime/market-watch.ts` parseQuote 函数（约 67 行）
- `src/skills/alphaos/plugins/dex-arbitrage.ts` evaluate 函数（约 82 行）
- `src/skills/alphaos/runtime/simulator.ts` estimate 函数（约 29 行）

**修复要求**：
- 在 parseQuote 中添加 `if (bid <= 0 || ask <= 0) return null`
- 在 dex-arbitrage evaluate 中添加价格正数校验
- 在 simulator estimate 中添加 grossEdgeBps 计算前的价格校验

## 🔴 Critical 2: claimPendingWhaleSignals 竞态
**位置**：`src/skills/alphaos/runtime/state-store.ts` 约 982 行

**修复要求**：
- 当前 UPDATE 已有 `AND status = 'pending'` 条件
- 需要添加乐观锁或唯一约束防止多实例重复消费
- 考虑添加 `updated_at` 字段 + 版本号检查，或使用 `RETURNING` 子句确认更新成功

修复后跑测试验证，确保 40 个测试全绿。

使用会话 ID: 019cb927-28bb-72f0-b19f-f494d36d3f1f (resume 模式)
