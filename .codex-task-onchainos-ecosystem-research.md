# Codex Task: 研究 OnchainOS / X 链生态里可直接复用的基础设施与默认配置

你现在不要改代码。

只做研究：从当前 AlphaOS / agent-comm 的目标出发，研究 **OnchainOS / X 链生态里有哪些现成可直接用的东西**，可以降低用户配置负担，同时避免把产品绑死成“只支持单一生态”。

## 研究目标
用户希望的方向是：
- **X 链作为推荐/default 路径**
- 但整体仍保持 **EVM-friendly**，不要把生态绑死
- 优先找出 OnchainOS / X 链生态中能直接复用的东西，减少用户需要手填和自建的配置

## 重点研究问题
请围绕下面这些问题给出结论：

### 1. 哪些配置项最可能由 OnchainOS / X 链生态直接提供或默认化？
例如但不限于：
- 推荐 RPC / 公共 RPC
- chainId / chainIndex
- token profile / token metadata
- 钱包 / AA / paymaster / relay / private submit 相关默认路径
- 认证方式默认值
- probe / integration status 可直接利用的能力

### 2. 当前 agent-comm / discovery 这条链路里，哪些用户配置是可以被“生态默认值”替代的？
请明确区分：
- 仍然必须用户自己提供的
- 可以由推荐默认值覆盖的
- 可以由脚本/API自动探测的

### 3. 从产品定位看，怎样表述最合适？
请给出判断：
- 什么应该写成 **X 链推荐路径**
- 什么应该保留为 **EVM 通用能力**
- 什么不应该在当前阶段承诺

### 4. 给出一份“配置减负建议清单”
请写成三栏：
- 当前配置项/前置条件
- 可否减负
- 推荐做法（默认值 / 自动探测 / 保持手填）

## 可用信息源
请优先基于：
- 当前仓库里的 `README.md`
- `docs/JUDGE_ONE_PAGER.md`
- `src/skills/alphaos/runtime/onchainos-client.ts`
- `src/skills/alphaos/runtime/config.ts`
- `src/skills/alphaos/api/server.ts`
- 以及你能从网页上确认到的 OnchainOS / X 链相关公开信息

## 输出要求
不要长篇大论。
按这个结构输出：

### 1. 可以直接借力的生态能力
- ...

### 2. 配置减负建议清单
- 配置项 | 可否减负 | 推荐做法

### 3. 推荐产品表述
- X 链推荐路径：...
- EVM 通用能力：...
- 当前不要承诺：...

### 4. 一句话结论
- 现在最值得优先落地的减负点是什么

要求：
- 客观
- 面向过程/结果/边界
- 不要把产品绑死到单一生态
- 但也不要回避 X 链作为默认推荐路径的现实价值
