# Codex Task: 复核并直接优化 TASK.md，然后直接开始实施

你现在要基于最新文档，先优化 `TASK.md`，然后**直接开始实施**，不需要等待新的人工确认。

## 先阅读
- `REQUIREMENT.md`
- `DESIGN.md`
- `TASK.md`

## 第一阶段：优化 TASK.md

请重点检查：
1. `TASK.md` 是否完全跟上最新 `DESIGN.md`
2. 是否遗漏了以下设计约束：
   - runtime/bootstrap 装配入口
   - `VAULT_MASTER_PASSWORD` 与 vault 钱包前置校验
   - runtime snapshot / 最小状态查询
   - inbound/outbound 状态流转
   - 落库前失败 vs 落库后失败的处理边界
3. 任务顺序是否合理，是否能直接支撑实施
4. 是否还有旧任务思维残留

你可以**直接修改 `TASK.md`**，让它更准确、更可实施。

完成后，在回复里简短说明：
- 你对 TASK.md 做了哪些关键调整
- 你准备从哪个任务开始实施

## 第二阶段：直接开始实施

在优化完 `TASK.md` 之后，**不要停**，直接按最新 `TASK.md` 开始实施。

实施原则：
- 从最合理的起始任务开始
- 优先推进能建立真实运行闭环的任务
- 每完成一小步就运行必要验证（至少 build / test）
- 避免引入 requirement / design 没要求的新能力

## 重要要求
- 允许直接修改代码、文档、配置示例
- 允许创建新文件（如 runtime/bootstrap 入口）
- 实施时保持边界收敛，不扩 scope
- 完成阶段性工作后，给出清晰进展摘要
