# Codex Task: 超聚焦确认 KISS 复用入口落地情况

你现在不要做全仓库 review，也不要改代码。

只做一个超聚焦确认：当前 agent-comm 的 KISS 复用入口到底落地到了什么程度。

## 重点问题
请只回答这 3 个问题：

### 1. 现在到底新增了哪些“可触达复用入口”？
请按入口类型列出：
- API
- CLI
- 内部 helper / runtime method
- 文档入口

重点是：用户/调用方现在到底能通过什么方式使用它，而不是底层有什么模块。

### 2. `ping` 和 `start_discovery` 是否都已经具备“足够薄”的发送入口？
请明确区分：
- `ping`：是否已具备薄入口
- `start_discovery`：是否已具备薄入口

这里的“薄入口”含义是：
- 用户不需要自己拼底层 tx-sender / peer-registry / crypto / codec
- 可以通过明确入口直接发起

### 3. 最小复用文档是否真的够用？
请判断当前文档是否已经足够支持别人完成这条路径：
1. 初始化 comm wallet
2. 查看 identity
3. 注册 peer
4. 发 ping
5. 发 start_discovery

如果不够，请直接指出缺口是什么。

## 请阅读的范围（尽量克制）
- `README.md`
- `docs/` 下与 agent-comm 相关内容
- `src/skills/alphaos/api/server.ts`
- `src/index.ts`
- `src/skills/alphaos/runtime/agent-comm/*`
- `tests/agent-comm-entrypoints.test.ts`
- 其他你认为必须看的极少量文件

## 输出格式
请严格按这个格式输出：

### 1. 已新增的可触达入口
- ...

### 2. `ping` 入口判断
- 结论：已具备 / 还不够
- 原因：...

### 3. `start_discovery` 入口判断
- 结论：已具备 / 还不够
- 原因：...

### 4. 文档可复用性判断
- 结论：够用 / 不够
- 缺口：...

### 5. 一句话结论
- 现在距离“别人可轻量复用”还差什么

要求：
- 客观
- 基于事实
- 尽量不要下潜太多无关代码
- 只回答这轮需要的结果
