# Codex Task: 生成 agent-comm 双实例手动闭环验证的最短方案

你现在不要改代码。

只做一件事：从当前已落地的 agent-comm v0.1 能力出发，给出一套**最短、最稳、最适合手动验证**的双实例闭环联调方案。

## 目标
验证两实例之间至少能实打实跑通：
1. `ping`
2. `start_discovery`

## 请从架构/流程角度梳理
不要深入无关代码，重点回答：

### 1. 双实例最小前置条件
需要准备什么：
- 两份 `DATA_DIR`
- 两套 comm wallet
- 哪些环境变量
- 哪些运行前置条件
- 是否需要两个不同端口
- 是否需要两个不同 API_SECRET
- 是否需要两个不同 peerId

### 2. 最短联调步骤
按最短路径列出步骤，例如：
- 初始化 A 钱包
- 初始化 B 钱包
- 查看双方 identity
- 互相注册 trusted peer
- 分别启动 runtime / server
- A 发 ping 给 B
- A 发 start_discovery 给 B
- 去哪里看结果

### 3. 每一步的验证点
每一步应该看什么来判断成功：
- API
- CLI
- message store
- status snapshot
- discovery session / report

### 4. 最容易踩坑的点
列出最重要的 5 个坑：
- 配置坑
- vault/钱包坑
- peerId/pubkey/address 对不上
- runtime 没启动
- 监听没连上链
- 发送走通但接收没执行

### 5. 一句话建议
当前是更适合：
- 手工命令联调
- 还是先补脚本

## 读取范围（尽量克制）
- `docs/AGENT_COMM_MIN_REUSE.md`
- `README.md`
- `src/index.ts`
- `src/skills/alphaos/api/server.ts`
- `src/skills/alphaos/runtime/agent-comm/entrypoints.ts`
- `src/skills/alphaos/runtime/agent-comm/runtime.ts`
- 如有必要，再读极少量相关文件

## 输出格式
严格按以下结构输出：

### 1. 双实例最小前置条件
- ...

### 2. 最短联调步骤
1. ...
2. ...

### 3. 每一步看哪里验证
- ...

### 4. 最容易踩坑的 5 个点
1. ...
2. ...

### 5. 建议
- ...

要求：
- 客观
- 实操导向
- 过程和结果导向
- 不要写成长篇设计文档
